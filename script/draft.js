// function useItem() {
//     let items = document.querySelectorAll(".item-container")
//     inventory.addEventListener("click", () => {
//         for (let item of items) {
//             console.log(item)
//             if (item.firstElementChild.dataset.use == "true") {
//                 activeTool.push(item.firstElementChild.dataset.type)
//             } else if (item.firstElementChild.dataset.use == "false") {
//                 activeTool.pop()
//             }
//         }
//         console.log(activeTool)
//     })
// }
// useItem()

        // let selected = [...gameBoard.children].filter ( (gameDiv) => {
        //     // if (lastPosition[lastPosition.length - 1] ===) {    
        //         return (gameDiv.dataset.x === lastPosition[lastPosition.length - 1]['x'] &&
        //         gameDiv.dataset.y === lastPosition[lastPosition.length - 1]['y'])
        //     // }
        // })
        // if (lastPosition[lastPosition.length - 1].type !== 'sky')